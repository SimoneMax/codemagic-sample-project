name: verbale_app_flutter
description: Verbali per il Consorzio di Bonifica
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.0.0
  firebase_auth: ^4.0.0
  cloud_firestore: ^4.0.0
  firebase_storage: ^11.0.0
  geolocator: ^10.0.0
  google_maps_flutter: ^2.2.0
  image_picker: ^1.0.4
  signature: ^5.2.1
  file_picker: ^6.1.1
  geojson: ^1.0.0
